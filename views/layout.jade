doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    script.
      globals = {
        nonProductionEnvironment: function() {
            //- TODO: until staging is fixed or replaced, send production mkto config
            //- if ('#{config.mode}' !== 'production') { 
            //-   return true;
            //- } else {
            //-   return false;
            //- }
            return false;
        },
        munchkin_id: '#{mktoConfig.munchkin_id}',
        formid: '#{mktoConfig.formids.testFormId}'
      }
      mktoTokens = {
          multistepify: true, 
          dynamicTCPA: true, 
          isUmbracoForm: false, 
          includePreviousButton: true, 
          tcpaToken: 'Fake TCPA token text'
      };
    //- mkto validation has a dependency on jquery :'(
    script(src='//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js')  
    script(src='//app-ab16.marketo.com/js/forms2/js/forms2.min.js')
    a(href="/") Home
    h2 mode: #{config.mode}, minchkin: #{mktoConfig.munchkin_id}
    block content
    script(src='/javascripts/mkto/lib.js')
    script(src="/javascripts/mkto/tests/helpers.js")    
    script(src='/javascripts/mkto/multistep.js')      
    block scripts